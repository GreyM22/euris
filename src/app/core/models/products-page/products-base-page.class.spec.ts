import { ProductsBasePage } from './products-base-page.class';

describe('ProductsBasePageDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductsBasePage();
    expect(directive).toBeTruthy();
  });
});
