import { AddToArrayPipe } from './add-to-array.pipe';

describe('AddToArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new AddToArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
